{% extends _layout %}

{% block CUSTOM %}
	<!-- Custom Css -->
    <link href="{{assets_backend_folder}}/css/custom.css" rel="stylesheet">
{% endblock %}

{% block CONTENT %}

	{{output}}

{% endblock %}




{% block SCRIPTS %}
  {% for csf in css_files %}
    <link rel="stylesheet" href="{{csf}}">
  {% endfor %}

  {% for jsf in js_files %}
    <script type="text/javascript" src="{{jsf}}"></script>
  {% endfor %}



<!-- <script type="text/javascript" src="http://bigbetpro.local/grocery_crud/texteditor/ckfinder/ckfinder.js"></script>
<script type="text/javascript" charset="utf-8">
	
var button1 = document.getElementById( 'field-image' );
button1.onclick = function() {
	selectFileWithCKFinder( 'field-image' );
};

function selectFileWithCKFinder( elementId ) {
	CKFinder.popup( {
		basePath : '/grocery_crud/texteditor/ckfinder',
		chooseFiles: true,
		width: 800,
		height: 600,
		onInit: function( finder ) {
			finder.on( 'files:choose', function( evt ) {
				var file = evt.data.files.first();
				var output = document.getElementById( elementId );
				output.value = file.getUrl();
			} );

			finder.on( 'file:choose:scaledImage', function( evt ) {
				var output = document.getElementById( elementId );
				output.value = evt.data.scaledUrl;
			} );
		}
	} );
}

</script> -->

{% endblock %}